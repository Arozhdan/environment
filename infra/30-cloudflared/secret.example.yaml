# Example only. Do not commit real tunnel credentials here.
# Create an encrypted `SopsSecret` under `secrets/cloudflared/` instead.
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-credentials
  namespace: cloudflared
type: Opaque
stringData:
  credentials.json: |
    {
      "AccountTag": "REPLACE_ME",
      "TunnelSecret": "REPLACE_ME",
      "TunnelID": "REPLACE_ME"
    }
