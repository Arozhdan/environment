apiVersion: apps/v1
kind: Deployment
metadata:
  name: folio
  namespace: folio
  labels:
    app.kubernetes.io/name: folio
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: folio
  template:
    metadata:
      labels:
        app.kubernetes.io/name: folio
    spec:
      imagePullSecrets:
        - name: folio-ghcr
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: folio
          image: ghcr.io/arozhdan/folio@sha256:a311feeddc1899ee2c3e83b2bb11fc47794083fa20250075f340b5cdf59f1569
          ports:
            - name: http
              containerPort: 8787
          env:
            - name: NODE_ENV
              value: production
            - name: DATA_DIR
              value: /data
            - name: ALLOWED_EMAIL
              value: arozhdan.dev@gmail.com
            - name: FOLIO_CONTENT_OWNER
              value: Arozhdan
            - name: FOLIO_CONTENT_REPO
              value: folio
            - name: FOLIO_CONTENT_BRANCH
              value: main
            - name: FOLIO_CONTENT_PREFIX
              value: folios/
            - name: CF_ACCESS_TEAM_DOMAIN
              value: arozhdan.cloudflareaccess.com
          envFrom:
            - secretRef:
                name: folio-runtime
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 768Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: folio-data
        - name: tmp
          emptyDir: {}
