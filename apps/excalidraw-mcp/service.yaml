apiVersion: v1
kind: Service
metadata:
  name: excalidraw-mcp
  namespace: excalidraw-mcp
spec:
  selector:
    app.kubernetes.io/name: excalidraw-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
